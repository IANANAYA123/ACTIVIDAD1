<svg width="300" height="300" viewBox="0 0 300 300" xmlns="http://www.w3.org/2000/svg">
  <!-- Fondo circular negro -->
  <circle cx="150" cy="150" r="150" fill="#000000"/>
  
  <!-- Persona simple -->
  <g transform="translate(150, 150)">
    <!-- Cabeza -->
    <circle cx="0" cy="-30" r="35" fill="#ffffff"/>
    
    <!-- Cuerpo simple -->
    <rect x="-25" y="10" width="50" height="60" rx="8" fill="#ffffff"/>
    
    <!-- Hombros -->
    <rect x="-35" y="5" width="70" height="20" rx="10" fill="#ffffff"/>
  </g>
  
  <!-- Texto -->
  <text x="150" y="220" text-anchor="middle" fill="#ffffff" font-family="Arial, sans-serif" font-size="20" font-weight="bold">IAN ANAYA</text>
  <text x="150" y="245" text-anchor="middle" fill="#ffffff" font-family="Arial, sans-serif" font-size="14">Ingeniero Industrial</text>
</svg>
